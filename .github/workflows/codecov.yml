name: Tests and code coverage
on: [push, pull_request]
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install LAPACK
        run: |
          sudo apt-get update && sudo apt-get install -y liblapack-dev
      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Install test dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-cov
      - name: Install ReCoN & tutorials in editable mode with compatibility settings
        run: |
          pip install -e .[tutorials] --config-settings editable_mode=compat
          pip install pybiomart
      - name: Run tests and collect coverage
        run: |
          pytest tests --cov=recon --cov-report=xml
      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}